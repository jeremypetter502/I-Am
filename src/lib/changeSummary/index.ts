export function diffProfiles(oldProfile: any, newProfile: any) {
  // Placeholder diff: return empty arrays — implement field-level diffing
  return { added: [], removed: [], updated: [], inferred: [] };
}
